const numbers = [25, 50, 75, 100];

for (let i = 0; i < numbers.length; i++) {
    // 配列に対して順に繰り返し処理が行われる
    console.log(`${i + 1}番: ${numbers[i]}`);
}

const persons = ['田中', '鈴木', '佐藤', '高橋'];

for (let i = 0; i < persons.length; i++) {
    // 配列に対して順に繰り返し処理が行われる
    console.log(`${i + 1}番: ${persons[i]}さん`);
}