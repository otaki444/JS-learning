// Math.abs(数値) 数値の絶対値を計算
Math.abs(-10); //-10の絶対値=10

// Math.pow(数値A, 数値B) 数値Aの数値B乗を計算する
Math.pow(2, 5); //2の5乗=32

// Math.sign(数値) 数値符号を計算する (-Xなら-1,Xなら1,0の場合は0)
Math.sign(2); //正の数なので1

// Math.sqrt(数値) 数値の平方根を計算
Math.sqrt(16); // 16の平方根=4

// Math.log(数値) 自然対数を計算
Math.log(Math.E); //eの自然対数=4