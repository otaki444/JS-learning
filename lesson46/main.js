const array = ['イチゴ', 'みかん', 'リンゴ'];

// 配列の長さを取得する
const arrayLength = array.length;

// 配列の各種について処理
for (let i = 0; i < arrayLength; i++) {
    // インデックスのiの要素を出力
    console.log(array[i]); // 'イチゴ', 'みかん', 'リンゴ'が順に出力
}